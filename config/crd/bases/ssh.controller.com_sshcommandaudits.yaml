---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: sshcommandaudits.ssh.controller.com
spec:
  group: ssh.controller.com
  names:
    kind: SshCommandAudit
    listKind: SshCommandAuditList
    plural: sshcommandaudits
    shortNames:
    - sca
    singular: sshcommandaudit
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.ssh\.monitor\.io/NodeName
      name: Node
      type: string
    - jsonPath: .metadata.labels.ssh\.monitor\.io/SourceIP
      name: SourceIP
      type: string
    - jsonPath: .status.totalCommands
      name: TotalCommands
      type: number
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SshCommandAudit is the Schema for the sshcommandaudits API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SshCommandAuditSpec defines the desired state of SshCommandAudit.
            properties:
              sshCommandEntity:
                description: SshCommandEntity is an Array  of SshCommandAudit.
                items:
                  properties:
                    command:
                      type: string
                    exit_code:
                      type: string
                    ip:
                      type: string
                    node:
                      type: string
                    port:
                      type: string
                    pwd:
                      type: string
                    time:
                      type: string
                    user:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: SshCommandAuditStatus defines the observed state of SshCommandAudit.
            properties:
              totalCommands:
                type: integer
            required:
            - totalCommands
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
